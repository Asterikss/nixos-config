{...}:

{
  services.displayManager.ly = {
    enable = true;
    settings = {
      animation = "matrix";
      animation_timeout_sec = 120;
      asterisk = "!";
      clear_password = true;
    };
  };
}
